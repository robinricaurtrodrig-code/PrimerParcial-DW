//Objeto Math

let resultado;

resultado = Math.PI; //PI
resultado = Math.round(2.5); //redondea
resultado = Math.ceil(2.1); //redondea hacia arriba
resultado = Math.floor(2.99); //redondea hacia abajo
resultado = Math.sqrt(144); //raiz cuadrada
resultado = Math.abs(-300); //convierte el numero en positivo
resultado = Math.min(3, 5, 2, 1, 10, 20); //Devuelve el valor minimo de un listado
resultado = Math.max(3, 5, 2, 1, 10, 20); //Devuelve el valor maximo de un listado
resultado = Math.random(); //genera un valor aleatorio entre 0 y 1
resultado = Math.floor(Math.random() * 30); //Combinación de funciones

console.log(resultado);